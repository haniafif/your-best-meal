export default interface initialData{
    content: [
        {
            id: Number,
            name: String,
            customer_rating: Number,
            distance: Number,
            price: Number,
            cuisine_id: number
        }
    ]
}